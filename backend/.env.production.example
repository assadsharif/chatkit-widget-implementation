# Production Environment Variables for Railway
# Copy to Railway environment variables (do not commit actual values)

# ==============================================
# Backend Configuration
# ==============================================

# CRITICAL: Generate a secure secret key
# Run: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
SECRET_KEY=your-production-secret-key-here

# Production mode (MUST be false in production)
INTEGRATION_TEST_MODE=false

# CORS Origins (Railway domain + custom domain if applicable)
# Example: https://chatkit-backend-production.up.railway.app
CORS_ORIGINS=https://your-railway-domain.railway.app,https://yourdomain.com

# ==============================================
# Neon Serverless PostgreSQL
# ==============================================

# Neon connection string (from Neon dashboard)
# Format: postgresql://user:password@host:port/database?sslmode=require&pgbouncer=true
# Example: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require&pgbouncer=true
DATABASE_URL=postgresql://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require&pgbouncer=true

# ==============================================
# Qdrant Cloud Vector Database
# ==============================================

# Qdrant cluster URL (from Qdrant Cloud dashboard)
# Format: https://cluster-id.region.cloud.qdrant.io:6333
# Example: https://7f8a9b3c-chatkit.aws.cloud.qdrant.io:6333
QDRANT_URL=https://your-cluster-id.aws.cloud.qdrant.io:6333

# Qdrant API Key (from Qdrant Cloud → API Keys)
# Create a new API key and copy here
QDRANT_API_KEY=your-qdrant-api-key-here

# Qdrant collection name for course content
# Default: physical_ai_course (auto-created on first run)
QDRANT_COLLECTION=physical_ai_course

# Embedding model for RAG (optional, defaults to all-MiniLM-L6-v2)
# Options: sentence-transformers/all-MiniLM-L6-v2 (384-dim, fast)
#          sentence-transformers/all-mpnet-base-v2 (768-dim, more accurate)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DIM=384

# Email Configuration (Optional - for production email sending)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=your-smtp-password
FROM_EMAIL=noreply@yourdomain.com

# Base URL for frontend (for email verification links)
BASE_URL=https://your-frontend-domain.com

# Rate Limiting (Production values)
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=20
RATE_LIMIT_SAVE_CHAT=10
RATE_LIMIT_PERSONALIZE=5
